#pragma once

#include <fstream>
#include <iostream>

namespace Helpers {
    std::string serialize(int num, int width) {
        oss << std::setw(width) << std::setfill('0') << number;
        return oss.str();
    }
}
